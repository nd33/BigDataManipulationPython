IMPORTANT
Read the list of variables and their meanings for every dataset before looking at the notebooks. They are included in the "report" folder.

HOW TO RUN
type: "env PATH=~/.local/bin:$PATH jupyter notebook" in terminal
if this does not work try typing "ipython3 notebook" or "ipython notebook".
